export type ConfigProps = {
  Sidebar_drawer: boolean;
  mini_sidebar: boolean;
  actTheme: string;
  fontTheme: string;
};

const config: ConfigProps = {
  Sidebar_drawer: true,
  mini_sidebar: false,
  actTheme: 'WhatsappLight',
  fontTheme: 'Public sans'
};

export default config;
